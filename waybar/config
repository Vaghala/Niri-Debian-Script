{
    // General Waybar settings
    "layer": "top",
    "position": "top",
    "height": 35,
    "spacing": 4,

    // Module configuration
    "modules-left": ["niri/workspaces"],
    "modules-center": ["clock"],
    "modules-right": ["network", "custom/launcher", "custom/power"],

    // Module-specific settings
    "niri/workspaces": {
        "all-outputs": true,
        "format": "{name}",
        "on-click": "activate"
    },

    "clock": {
        "format": "{:%A, %d %B - %H:%M}",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>"
    },

    "network": {
        "format-wifi": "{essid} ({signalStrength}%) ",
        "format-ethernet": "{ifname}: {ipaddr}/{cidr} ",
        "format-disconnected": "Disconnected ⚠",
        "tooltip-format": "{ifname} via {gwaddr} ",
        "on-click": "nm-connection-editor" // Opens network manager GUI
    },
    
    "custom/launcher": {
        "format": "", // Icon for Fuzzel (Nerd Font: nf-mdi-apps)
        "on-click": "fuzzel",
        "tooltip": true,
        "tooltip-text": "Application Launcher"
    },

    "custom/power": {
        "format": "", // Icon for Power Menu (Nerd Font: nf-fa-power_off)
        "on-click": "wlogout",
        "tooltip": true,
        "tooltip-text": "Power Menu"
    }
}
